<p id="notice"><%= notice %></p>

<h1><先生></h1>

<br>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th width=250px><center>email</center></th>
      <th width=200px><center>名前</center></th>
      <th width=100px><center>管理者</center></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><center><%= teacher.user.email %></center></td>   
        <td><center><%= teacher.name %></center></td>
        <td><center><%= teacher.admin %></center></td>
        <td width=65px><center><%= link_to '詳細', teacher %></center></td>
        <td width=65px><center><%= link_to '編集', edit_teacher_path(teacher) %></center></td>
        <td width=65px><center><%= link_to '削除', teacher, method: :delete, data: { confirm: "本当に削除しますか？" } %></center></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<br>
<br>

<table>
<td><pre style="font-size: 20px;"><%= link_to '新規作成', new_teacher_path %></td>
</table>
