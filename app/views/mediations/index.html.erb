<p id="notice"><%= notice %></p>

<h1><斡旋方法></h1>

<br>

<table class="table table-striped table-bordered">
  <thead bgcolor="lightsteelblue">
    <tr>
      <th>斡旋方法</th>
    </tr>
  </thead>

  <tbody bgcolor="white">
    <% @mediations.each do |mediation| %>
      <tr>
        <td><%= mediation.name %></td>
        <td><center><%= link_to '詳細', mediation %></center></td>
        <td><center><%= link_to '編集', edit_mediation_path(mediation) %></center></td>
        <td><center><%= link_to '削除', mediation, method: :delete, data: { confirm: 'Are you sure?' } %></center></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<br>

<table>
<td><pre style="font-size: 13px;"><%= link_to '新規作成', new_mediation_path %></td>
</table>
