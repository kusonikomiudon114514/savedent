<%= form_with(model: saw, local: true) do |form| %>
  <% if saw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saw.errors.count, "error") %> prohibited this saw from being saved:</h2>

      <ul>
      <% saw.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :saw_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
