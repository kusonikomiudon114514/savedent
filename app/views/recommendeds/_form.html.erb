<%= form_with(model: recommended, local: true) do |form| %>
  <% if recommended.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recommended.errors.count, "error") %> prohibited this recommended from being saved:</h2>

      <ul>
      <% recommended.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :推薦形態 %>
    <%= form.text_field :name, id: :recommended_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
