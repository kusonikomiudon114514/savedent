<p id="notice"><%= notice %></p>

<h1><面接方法></h1>

<br>

<table>
  <thead>
    <tr>
      <th>面接方法</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interviews.each do |interview| %>
      <tr>
        <td><%= interview.name %></td>
        <td><%= link_to '詳細', interview %></td>
        <td><%= link_to '編集', edit_interview_path(interview) %></td>
        <td><%= link_to '削除', interview, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<br>

<table>
<td><pre style="font-size: 13px;"><%= link_to '新規作成', new_interview_path %></td>
</table>
